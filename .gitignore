# ─── Secretos — NUNCA versionar ───────────────────────────
*.env
.env
.env.*
*.creds
creds.json
config.secrets.json
*.token
secrets.*
**/user_data/config.json
**/config.json

# ─── Datos de mercado (se descargan, no se versionan) ─────
**/user_data/data/
**/*.feather
**/*.json.gz
.backtest_data.json

# ─── Estado runtime ───────────────────────────────────────
**/user_data/trades/
**/user_data/logs/
**/*.log
state.json
state.json.*

# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.pyc

# ─── OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Fase D: raw trades OOS (regenerable desde zips freqtrade CT 113) ──
results/trades_B.json
# ─── Fase D (carry): raw funding data (regenerable via scripts/carry_backtest.py --fetch) ──
results/funding_raw.json
results/longcandidates/

# ─── Fase B.3: klines BTC (proxi régimen, regenerable desde CT 113 feather) ──
results/btc_2h.csv
