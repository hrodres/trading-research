# trading-research — dependencias de análisis (scripts/)
# freqtrade corre en su propio contenedor Docker (imagen freqtradeorg/freqtrade:stable)
# con dependencias fijadas por la imagen; aquí solo lo que corremos localmente.
pandas>=2.0
numpy>=1.24
pyarrow>=14.0   # lectura de klines .feather generados por freqtrade
pytest>=7.0     # tests/ (CI)
